# Experiments

Each subfolder is a self-contained study exercise. Name them with a numeric prefix for ordering, e.g.:

- `01-basic-prompt/`
- `02-image-to-image/`

Run a script with:

```bash
# from 04-langchain-image-generation/
npx tsx examples/01-basic-prompt/index.ts
```
